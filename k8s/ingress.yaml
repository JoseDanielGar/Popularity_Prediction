apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: gateway-ingress-8
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /dashboard
            pathType: Prefix
            backend:
              service:
                name: dashboard-app-service
                port:
                  number: 3000
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: song-popularity-api-service
                port:
                  number: 3000
